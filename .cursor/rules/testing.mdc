---
description: 测试规范。编写或修改测试文件时自动生效。
globs: "**/test_*.py,**/tests/**/*.py,**/*_test.py"
---

# 测试规范

## 框架

- 使用 `pytest` 作为测试框架
- 使用 `pytest-asyncio` 测试异步代码
- 使用 `pytest-cov` 统计覆盖率

## 测试结构

- 测试文件命名: `test_<module>.py`
- 测试函数命名: `test_<行为描述>_<预期结果>`
- 使用 `arrange / act / assert` 三段式组织测试

```python
def test_create_agent_with_valid_config_returns_agent_id():
    # Arrange
    config = AgentConfig(name="test-agent", runtime="docker")

    # Act
    result = create_agent(config)

    # Assert
    assert result.agent_id is not None
    assert result.status == "created"
```

## 覆盖率要求

- 新增代码覆盖率 >= 80%
- 核心模块（调度器、网关、状态机）覆盖率 >= 90%
- 覆盖率不足时必须说明原因

## Mock 原则

- 只 Mock 外部依赖（HTTP 调用、数据库、消息队列），不 Mock 内部逻辑
- 使用 `unittest.mock.patch` 或 `pytest-mock`
- Mock 对象必须验证调用参数，不能只验证"被调用过"

## 测试分层

| 层级 | 范围 | 速度 | 占比 |
| :--- | :--- | :--- | :--- |
| 单元测试 | 单个函数/类 | 毫秒级 | 70% |
| 集成测试 | 模块间交互 | 秒级 | 20% |
| 端到端测试 | 完整流程 | 分钟级 | 10% |
