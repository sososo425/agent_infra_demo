---
description: 安全红线规则。最高优先级，适用于所有文件。
alwaysApply: true
---

# 安全红线

以下规则为**强制约束**，任何情况下不得违反。

## 禁止硬编码敏感信息

- 绝对禁止在代码中硬编码 API Key、Token、Password、Secret
- 敏感配置通过环境变量或配置中心注入
- 如果代码示例需要展示密钥格式，使用 `<YOUR_API_KEY>` 占位

```python
# ❌ 禁止
API_KEY = "sk-abc123..."

# ✅ 正确
API_KEY = os.environ["API_KEY"]
```

## 输入验证

- 所有外部输入（用户请求、API 参数、配置文件）必须做校验
- 使用 `pydantic` 做数据模型验证
- 不信任任何来自网络的数据

## 依赖安全

- 禁止引入来源不明的第三方库
- 新增依赖前检查其维护状态（最近更新时间、issue 数量、星标数）
- 关注已知漏洞（可通过 `pip-audit` 扫描）

## 文件操作

- 禁止使用 `eval()`、`exec()` 执行动态代码
- 文件路径操作必须做路径遍历检查（防止 `../../etc/passwd`）
- 临时文件使用 `tempfile` 模块，确保清理

## 网络安全

- HTTPS only（禁止 HTTP 明文传输敏感数据）
- API 调用必须设置超时（connect_timeout + read_timeout）
- 日志中禁止打印完整的敏感信息（Token 只打印前 4 位）
